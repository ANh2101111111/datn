// import { create } from "zustand";

// // Store quản lý số lượng sản phẩm trong giỏ hàng
// interface CartStore {
//   cartCount: number;
//   incrementCartCount: () => void;
// }

// export const useCartStore = create<CartStore>((set) => ({
//   cartCount: 0,
//   incrementCartCount: () => set((state) => ({ cartCount: state.cartCount + 1 })),
// }));
